// import React from 'react';
// import {useSelector} from 'react-redux';
//

let primary = '#C1403D';
let accent = '#ffeaea';

export const changeColors = color => {
  primary = color;
  console.log(primary);
};

export default {
  primary,
  accent,
};
